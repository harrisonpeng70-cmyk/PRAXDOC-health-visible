# 《健康可见 1.0 云端公共知识库》联调预检报告 v1.1

## 1. 执行时间
- 日期：2026-04-12
- 执行环境：Windows 本机 PostgreSQL 18

## 2. 连接参数（已验证）
- `PGHOST=localhost`
- `PGPORT=5432`
- `PGUSER=postgres`
- `PGDATABASE=postgres`

## 3. 迁移执行结果
### 3.1 原始迁移
- 文件：`健康可见_1.0_知识库_数据库迁移_v1.1.sql`
- 结果：失败
- 原因：`extension "vector" is not available`

### 3.2 兼容迁移
- 文件：`健康可见_1.0_知识库_数据库迁移_v1.1_无pgvector兼容.sql`
- 结果：成功（`COMMIT`，`EXIT_CODE=0`）
- 处理：跳过 `vector` 扩展与向量索引，将 `embedding` 暂存为 `double precision[]`

## 4. 种子数据执行结果
- 文件：`健康可见_1.0_知识库_最小种子数据_v1.1.sql`
- 结果：成功（`COMMIT`，`EXIT_CODE=0`）

## 5. 数据完整性校验
- `kb_tenants = 1`
- `kb_sources = 3`
- `kb_entries = 5`
- `kb_entry_versions = 6`
- `kb_conflicts = 1`
- `kb_query_logs = 1`
- `kb_audit_logs = 3`
- 视图 `kb_online_entry_versions` 在线记录数：`5`

## 6. 检索烟雾测试（按 v1.1 评分规则）
- 查询词：`glucose screening followup`
- 结果 Top1：`KB-L1-DM-0002`（L1，procedure）
- Top2：`KB-L1-DM-0001`（L1，judgment）
- 结论：分层权重与证据权重生效，L1 优先符合预期

## 7. 当前可开发状态
1. 可立即进入 API 开发与联调（入库/检索/审计）。
2. 可使用 Postman 集合做端到端验证。
3. 向量检索暂以“占位分”运行，待 pgvector 安装后切换真实向量召回。

## 8. 下一步建议（必须）
1. 安装 `pgvector` 到 PostgreSQL 18。
2. 执行 `健康可见_1.0_知识库_pgvector补齐迁移_v1.1.sql`。
3. 回归验证：向量索引、检索时 `R_vector` 实值是否生效。
